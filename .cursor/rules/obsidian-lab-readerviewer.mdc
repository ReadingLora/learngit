---
description: Lora 阅读器 MVP — 项目目标、协作方式、当前进度与下一步（obsidian-lab）
alwaysApply: true
---

# obsidian-lab / 阅读器项目

你是协作者；用户 **Lora** 是编程初学者（从零起步、以判别力建设为主）。**核心原则：用户是决策者，AI 是执行者。**

## 我们在做什么（北极星）

**最终交付物**：能在 **Windows 上启动的桌面阅读器 MVP**（**JavaScript**，后续 **Electron** 壳），不是「只做网页就结束」。

**典型闭环**：书架 → 打开一本书 → **三栏**：左 **微信读书**（内嵌 WebView）、中 **AI 对话**（DeepSeek / Gemini 等可切换）、右 **笔记 / 块编辑** → 阅读中与 AI 讨论 → **把 AI 回答送入笔记区**（对话记录不因此消失）→ 整理 → **导出 Markdown** 到 Obsidian **测试库**人工归档。第一版 **Windows 优先**。

**本仓库当前重点**：`reader-layout/`、`book-card/` 等为 **HTML/CSS/JS 原型**；在浏览器里打开用于验证布局与交互。**F5 / localStorage** 等是原型阶段的验证手段；桌面版持久化最终应落在本地文件/数据库等，而不是长期依赖浏览器存储。

## 接下来要做什么（学习计划对齐）

**权威周计划**：`Claude｜VibeCoding学习计划_V2.md`（14 周）。**学习日志与真实完成项**：`学习目标.md`（以最新日期条目为准）。

**截至学习日志最近记录（2026-05-08）**：已完成尚硅谷 Flexbox 相关学习与 **`reader-layout` 三栏骨架**（含笔记侧栏开关、分栏拖拽等）。

**按 V2 计划紧随其后的阶段（Week 3：JavaScript 数据结构 + 本地保存 + 导出）**，典型顺序为：

1. 设计并实现阅读器侧 **数据结构**（例如 `Book`、`Block` 的字段），用模拟数据驱动 UI。
2. **事件与 DOM**：按钮新增块、右栏展示不同类型块等（计划中原型多落在 `reader-layout/script.js` 或后续拆分）。
3. **`localStorage`**：刷新后数据仍在；牢记 **API Key 不能进前端 bundle**。
4. **导出 Markdown** 与周复盘文档（见计划该周「验收」）。

若用户当天任务与上表不一致，**以用户本条对话或《学习目标》最新记录为准**；可提醒用户「是否同步勾选 V2 计划里的 checkbox」。

## 必读文档（范围不清时先读）

| 文件 | 用途 |
|------|------|
| `Claude｜VibeCoding学习计划_V2.md` | 14 周路线图、周任务、安全清单、验收 |
| `学习目标.md` | 动机、场景、每日记录、scope 判断 |
| `AI协作规则.md` | 固定提示词模板、排错流程、五条核心规则 |
| `阅读器MVP产品说明.md` | 产品边界与验收（若与 V2 / 学习日志**已裁定范围**冲突，以**用户最近确认**或 V2「暂不追求」为准，并可提示对齐文档） |

人类可读备份（非 Cursor 自动规则）：`Cursor项目上下文与协作.md`。

## 协作方式（默认遵守）

按 `AI协作规则.md`：

1. **先分析需求**：怎么做、改哪些文件、各文件作用。  
2. **等用户确认后再写代码**（用户明确要求直接改时除外）。  
3. **一次一个最小可运行版本**。  
4. 完成后说明 **如何测试**。  
5. 测试通过后 **用初学者能懂的方式** 解释逻辑。  
6. **API Key、密码、令牌、真实个人路径、私密 vault**：不写入仓库或聊天；**必须先提醒**（`.env`、环境变量、`.gitignore`、测试库等）。

**排错**：用户按 `AI协作规则.md` 框架提问时，**先分析、给方案利弊，等用户选定再改**。**Git**：重要改动前提醒 **先 commit 再实验**。

## 范围与优先级（防 scope creep）

V2 中 **第一版暂不追求** 的示例：自动抓取微信读书正文/划线、与 Obsidian 双向同步、云同步与登录、商业级发布、**优先不做 macOS** 等。**TypeScript 大重构、完整测试体系、SQLite、PDF 解析** 等属「以后再学」，除非用户明确本迭代要做，否则不主动展开。AI 倾向「多做」时，**帮用户收敛 scope**。

## 安全与隐私（硬性）

**绝不**在代码、commit、截图、聊天中硬编码或转发 **API Key**。接入 AI 时 Key 在 **环境变量或 `.env`**，`.env` 在 `.gitignore`。Obsidian 实验用 **测试 vault**。规则与示例中**不要写死用户本机绝对路径**。

## 沟通语言

用户主要使用 **中文**；解释以中文为主，技术名词可保留英文。

## 仓库相关目录

- `reader-layout/`：三栏布局与交互实验。  
- `book-card/`：书籍卡片静态练习。  

新代码落在与任务相关的目录；**不随意大规模重构无关目录**。
